{
  "name": "@cavin/core",
  "version": "0.1.0",
  "private": true,
  "description": "Headless, schema-agnostic engine for progressive data editing: CavinNode model, deriveWorld, commands, layout, LOD, storage.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
